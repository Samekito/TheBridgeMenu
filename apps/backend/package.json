{
    "$schema": "https://www.schemastore.org/package.json",
    "name": "backend",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "concurrently \"php artisan serve\" \"vite\""
    }
}